/* eslint-disable comma-dangle */
import { ReactNode } from 'react'

interface IProps {
  children: ReactNode
}
export const LayoutBlog = ({ children }: IProps) => {
  return <div className="bg-[#171A1D]"></div>
}
